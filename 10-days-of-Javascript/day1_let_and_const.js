function main() {
    const PI = Math.PI;
    let r = parseFloat  (readLine());
    let area = PI * Math.pow(r,2);
    console.log(area);        
    var perimeter = 2 * PI * r;
    console.log(perimeter);
